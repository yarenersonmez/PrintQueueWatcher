<Window x:Class="PrintQueueWatcher.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Str_AppTitle}"
        Height="470" Width="480"
        MinHeight="470" MinWidth="480"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource Brush_WindowBackground}"
        Icon="pack://application:,,,/Resources/app.ico">

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Başlık -->
        <TextBlock Grid.Row="0"
                   Text="{DynamicResource Str_AppTitle}"
                   FontSize="24" FontWeight="Bold"
                   Foreground="{DynamicResource Brush_PrimaryText}"
                   Margin="0,0,0,24"/>

        <!-- Seçili yazıcı bilgisi -->
        <Border Grid.Row="1"
                Background="{DynamicResource Brush_PanelBackground}"
                BorderBrush="{DynamicResource Brush_BorderColor}"
                BorderThickness="1" CornerRadius="6"
                Padding="16" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="{DynamicResource Str_SelectedPrinterLabel}"
                           Style="{StaticResource HintText}"/>
                <TextBlock x:Name="PrinterNameText"
                           Text="{DynamicResource Str_NoPrinterSelected}"
                           FontSize="16" FontWeight="SemiBold"
                           Foreground="{DynamicResource Brush_PrimaryText}"
                           Margin="0,4,0,0" TextTrimming="CharacterEllipsis"/>
            </StackPanel>
        </Border>

        <!-- Durum satırı -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,24">
            <Ellipse x:Name="StatusDot" Width="10" Height="10" Fill="Gray" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBlock x:Name="StatusText"
                       Text="{DynamicResource Str_StatusIdle}"
                       Style="{StaticResource LabelText}"/>
        </StackPanel>

        <!-- Başlat/Durdur butonu -->
        <StackPanel Grid.Row="3" HorizontalAlignment="Center" Margin="0,0,0,4">
            <Button x:Name="StartStopButton"
                    Content="{DynamicResource Str_StartButton}"
                    Style="{StaticResource PrimaryButton}"
                    Width="200" Height="44"
                    Click="StartStopButton_Click"/>
            <TextBlock Text="{DynamicResource Str_TrayHint}"
                       Style="{StaticResource HintText}"
                       HorizontalAlignment="Center"
                       TextAlignment="Center"
                       Margin="0,10,0,0"/>
        </StackPanel>

        <!-- Boşluk dolgusu -->
        <Grid Grid.Row="4"/>

        <!-- Alt satır: Windows ile başlat + Ayarlar -->
        <Grid Grid.Row="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="{DynamicResource Str_MinimizeToTrayHint}"
                       Style="{StaticResource HintText}"
                       TextAlignment="Left"
                       Margin="0,0,0,10"/>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <CheckBox x:Name="StartWithWindowsCheckBox"
                          Grid.Column="0"
                          Content="{DynamicResource Str_StartWithWindows}"
                          VerticalAlignment="Center"
                          Checked="StartWithWindowsCheckBox_Changed"
                          Unchecked="StartWithWindowsCheckBox_Changed"/>

                <Button Grid.Column="1"
                        Content="{DynamicResource Str_SettingsButton}"
                        Style="{StaticResource SecondaryButton}"
                        Click="SettingsButton_Click"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
